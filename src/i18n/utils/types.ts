export type LangTypes = {
    [key: string]: any;
};

export type ServicesTypes = {
    id: string;
    title: string;
    short: string;
    description: string;
    image: string;
    icon: string;
    features: string[];
};